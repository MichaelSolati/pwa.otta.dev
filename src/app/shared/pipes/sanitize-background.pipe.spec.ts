import { SanitizeBackgroundPipe } from './sanitize-background.pipe';

describe('SanitizeBackgroundPipe', () => {
  it('create an instance', () => {
    const pipe = new SanitizeBackgroundPipe();
    expect(pipe).toBeTruthy();
  });
});
